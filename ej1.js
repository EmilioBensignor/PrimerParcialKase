let nombre;
let edad;
let genero;

// genero = m
// genero == m
// genero === m

// Banner
// if edad >= 18 && genero === 'f'
// if edad >= 18 && genero === 'm'
// if edad < 18 && genero === 'f'
// if edad < 18 && genero === 'm'
// if edad < 0 || edad > 105 || !edad || isNan(edad)
//    banner5
//    pastillas mix
//    color es tal

// Juan para vos
// if nombre === "" || nombre === numero

// Las pastillas del abuelo
// if edad >= 18
// if edad >= 13 && edad <= 17
// if edad < 13
// if edad < 0 || edad > 105 || !edad || isNan(edad)

// if genero === 'f'
// if genero === 'm'

// if alguno invalid -> alert